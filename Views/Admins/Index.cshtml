@model IEnumerable<SportsZone_using_CoreWebAPI.Models.Admin>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AdmName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AdmEmail)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AdmPassword)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AdmName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AdmEmail)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AdmPassword)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AdmId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AdmId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AdmId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
